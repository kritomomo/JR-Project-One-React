import React from 'react';
import './Avatar.css';

const Avatar = ({ 
    img,
}) => (
    <>
        {img.map(({
            img,
        }) => (
            
        ))}
        
    </>
)

export default Avatar;