import './ContactInfo.css';

const ContactInfo = ({
    items,
}) => (
    <>
        {items.map(({
            key,
            icon,
            text,
        }) => (

        ))}

    </>
)

export default ContactInfo;